describe('Scrapes a URL based on JSON configuration', function() {
    it('validates a JSON contract')
    it('sets scraping options based on a valid JSON contract')
    it('scrapes a URL and returns an array of scraped items')
})
